# LangChain dependencies for enhanced PDF processing and agent framework
langchain>=0.1.0
langchain-community>=0.0.20
langchain-core>=0.1.0
unstructured>=0.10.0
unstructured[pdf]>=0.10.0

# Agent and tool dependencies
pydantic>=2.0.0
httpx>=0.24.0

# Additional dependencies for better PDF processing
python-magic>=0.4.27
python-magic-bin>=0.4.14  # For Windows
pillow>=9.0.0
pdfplumber>=0.9.0

# Optional: For better text processing
nltk>=3.8
spacy>=3.7.0

# Performance improvements
aiofiles>=23.0.0
asyncio-throttle>=1.0.0

# SSE support (should already be available)
sse-starlette>=1.6.0